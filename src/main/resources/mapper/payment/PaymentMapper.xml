<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plateer.ec1.payment.mapper.PaymentMapper">

   <select id="getOpPayInfo" resultType="com.plateer.ec1.common.model.order.OpPayInfo">
      SELECT
          pay_no, ord_no, pay_mn_cd, pay_ccd,
          pay_prgs_scd, pay_amt, cncl_amt, rfnd_avl_amt,
          trsn_id, vr_val_dt , vr_acct, vr_acct_nm,
          vr_bnk_cd, SYS_reg_dtime, sys_regr_id, sys_mod_dtime, sys_modr_id
        FROM op_pay_info
       WHERE ord_no = #{ordNo};
   </select>

   <select id="getOpOrdBase" resultType="com.plateer.ec1.common.model.order.OpOrdBase">
      SELECT ord_no,
             rfnd_bnk_ck,
             rfnd_acct_no,
             rfnd_acct_own_nm
        FROM op_pay_info
       WHERE ord_no = #{ordNo};
   </select>
</mapper>