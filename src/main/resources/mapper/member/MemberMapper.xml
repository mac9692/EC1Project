<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.plateer.ec1.member.mapper.MemberMapper">
    <select id="getMemberList" resultType="com.plateer.ec1.member.vo.Member">
        SELECT *
        FROM et_mbr_base
        ORDER BY mbr_no DESC;
    </select>
</mapper>