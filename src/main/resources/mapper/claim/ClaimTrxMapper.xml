<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plateer.ec1.claim.mapper.ClaimTrxMapper">
    <insert id="insertMonitoringLog" parameterType="com.plateer.ec1.common.model.order.OpOrdClmMntLogModel" useGeneratedKeys="true" keyProperty="logSeq">
        INSERT INTO op_ord_clm_mnt_log
        (
        ord_no, req_pram, sys_reg_dtime, sys_regr_id, sys_mod_dtime, sys_modr_id
        )
        VALUES
        (
        #{ordNo}, (#{reqPram})::json,
        now(), 'admin', now(), 'admin'
        )
    </insert>

    <update id="updateMonitoringLog" parameterType="com.plateer.ec1.common.model.order.OpOrdClmMntLogModel">
        UPDATE op_ord_clm_mnt_log
        SET ins_data = (#{insData})::json
          , sys_mod_dtime = now()
          , sys_modr_id = 'admin'
        WHERE log_seq = #{logSeq}
    </update>
</mapper>